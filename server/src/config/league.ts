/** Number of clubs in the league. */
export const LEAGUE_SIZE = 20;

/** Number of matchdays per season (round-robin). */
export const MATCHDAYS_PER_SEASON = 38;

/** Fixtures per matchday (LEAGUE_SIZE / 2). */
export const FIXTURES_PER_MATCHDAY = LEAGUE_SIZE / 2;
