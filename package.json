{
  "name": "fulltime",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "bun run --filter server dev & bun run --filter client dev",
    "dev:server": "bun run --filter server dev",
    "dev:client": "bun run --filter client dev",
    "seed": "bun run --filter server seed",
    "build": "bun run --filter client build",
    "test": "bun run --filter server test && bun run --filter client test",
    "test:server": "bun run --filter server test",
    "test:client": "bun run --filter client test"
  }
}
